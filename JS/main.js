/*for (var numeros = 0; numeros <= 100; numeros++) {
    if (numeros % 2 === 0) { //si es vedad lo que esta entre los perentecis, se ejecuta lo que esta adentro de las llaves
        console.log(`numero: ${numeros} es numero par`)
    }
    else { // si lo que estra adentro de if () anterior no se cumple se ejecuta en else dentro de {}  
        console.log(`${numeros} no es par`)
    }
}
*/
var datos = [p, e, r, r, o]
for (var index = 0; index < datos.length; index++) {
    console.log(`${datos[index][0]} ${datos[index][4]}`)
}